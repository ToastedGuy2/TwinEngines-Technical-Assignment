const host = "https://localhost:5001";
const apiBaseUrl = `${host}/api`;
const customersApiUrl = `${apiBaseUrl}/customers`;
const typesApiUrl = `${apiBaseUrl}/types`;
export { customersApiUrl, typesApiUrl };
